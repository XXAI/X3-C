import { FilterInsumosPipe } from './filter-insumos.pipe';

describe('FilterInsumosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInsumosPipe();
    expect(pipe).toBeTruthy();
  });
});
